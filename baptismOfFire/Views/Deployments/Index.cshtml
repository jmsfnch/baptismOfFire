@model IEnumerable<baptismOfFire.Models.Deployment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CreateDate)
            @Html.DisplayNameFor(model => model.Certificate.CN)
            @Html.DisplayNameFor(model => model.Certificate.ExpiresDate)
            @Html.DisplayNameFor(model => model.Server.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDate)
            @Html.DisplayFor(modelItem => item.Certificate.CN)
            @Html.DisplayFor(modelItem => item.Certificate.ExpiresDate)
            @Html.DisplayFor(modelItem => item.Server.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
